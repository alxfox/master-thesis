# number of frames used = multi_adj_frame_id_cfg[1] - multi_adj_frame_id_cfg[0]

model:
  loss_depth_weight: 3.0
  # encoders:
  #   camera:
  #     vtransform:
  #       depthnet_cfg:
  #         use_dcn: false
  #         aspp_mid_channels: 96
  #       type: BEVDepthLSSTransform